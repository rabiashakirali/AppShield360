version: '3.8'
services:
  appshield360:
    build: .
    container_name: appshield360
    volumes:
      - ./reports:/app/reports
    ports:
      - "5000:5000"
    environment:
      - PYTHONUNBUFFERED=1
    command: python dashboard.py